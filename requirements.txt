# ============================================================
# SMB Intelligence Platform — Requirements (Python 3.14 compatible)
# GenAI Academy APAC Hackathon | TiGeR / Adarsh Gautam
# ============================================================

# --- Data Generation (Part 1) ---
pandas>=2.2.0
numpy>=2.0.0
Faker>=22.0.0
pyarrow>=14.0.0

# --- Google Cloud (Parts 1, 3) ---
google-cloud-storage>=2.14.0
google-cloud-bigquery>=3.17.0
google-cloud-bigquery-storage>=2.24.0
db-dtypes>=1.2.0

# --- FastAPI Backend (Part 4) ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9
httpx>=0.27.0

# --- ML & Risk Scoring (Part 3) ---
xgboost>=2.0.0
scikit-learn>=1.4.0
joblib>=1.3.0

# --- Utilities ---
python-dotenv>=1.0.0
tqdm>=4.66.0
requests>=2.31.0
tabulate>=0.9.0

# --- cuDF / RAPIDS (Part 2) ---
# Install SEPARATELY via conda (requires CUDA GPU):
#   conda install -c rapidsai -c conda-forge -c nvidia cudf python=3.10 cuda-version=12.0
# Note: cuDF requires Python 3.10/3.11 + CUDA GPU.
#       pipeline_cudf.py falls back to pandas automatically when unavailable.
